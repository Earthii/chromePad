{
  "name": "chromePad",
  "version": "1.0.1",
  "description":
    "A chrome extension which allows user to store notes into the browser. This is powered by an rich text editor and can store multiple notes",
  "browser_action": {
    "default_icon": "assets/icon.png",
    "default_popup": "index.html"
  },
  "content_security_policy":
    "script-src 'self' 'unsafe-eval'; object-src 'self'",
  "permissions": ["storage"],
  "background": {
    "scripts": ["scripts.js"],
    "persistent": false
  },
  "manifest_version": 2
}
